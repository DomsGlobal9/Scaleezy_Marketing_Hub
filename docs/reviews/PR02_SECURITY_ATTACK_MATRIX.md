# PR02_SECURITY_ATTACK_MATRIX.md

Do not mark PASS from code inspection alone when an automated test is practical.

All named tests live in `Marketing_backend/apps/inspirations/tests.py` and pass in the run recorded in `docs/reviews/PR02_SELF_REVIEW.md`.

Negative assertions run through the shared helpers in `Marketing_backend/apps/common/testing.py`, so every rejection below proves the response **and** that the database did not move — a 400 with a row written would fail the same test.

| Attack ID | Attack | Applicable? | Expected | Test/evidence | Result |
|---|---|---|---|---|---|
| TEN-01 | Tenant A references Tenant B brand | Yes | Reject/no write | `test_cross_tenant_brand_injection_blocked` (400, asserts no row for brand2); `test_upload_cross_tenant_brand_injection_blocked` (400, asserts `BrandInspiration.objects.exists()` is False) | PASS |
| TEN-02 | Tenant A references Tenant B source | Yes | Reject/no write | `test_cross_tenant_source_injection_blocked`, `test_upload_cross_tenant_source_injection_blocked` | PASS |
| TEN-03 | Tenant A attaches a signal to Tenant B inspiration | Yes | Reject/no write | `test_signal_cannot_attach_to_other_tenant_inspiration` (400, asserts `inspiration.signals.count() == 0`) | PASS |
| TEN-04 | Guess another tenant's object ID | Yes | 404/deny | `test_inspiration_is_hidden_from_the_other_tenant`, `test_signal_is_hidden_from_the_other_tenant` | PASS |
| TEN-05 | Staff user without workspace membership | Yes | Deny | `test_staff_without_membership_cannot_read` (403/404; PR0-001) | PASS |
| TEN-06 | Alternate mutation paths for a foreign object (PATCH, PUT, archive, analyze, confirm, reject, signal PATCH) | Yes | 404 on every path, no state change | `test_every_mutation_path_is_404_for_another_tenant` (7 paths asserted, then re-reads both rows) | PASS |
| BR-01 | Brand A references Brand B source inside one workspace | Yes | Reject | `test_cross_brand_source_injection_blocked_inside_one_workspace`, `test_upload_cross_brand_source_injection_blocked` | PASS |
| BR-02 | PATCH only the relation to produce an invalid final graph | Yes | Reject | `test_patch_cannot_move_inspiration_to_another_brand`, `test_patch_cannot_move_inspiration_to_another_tenant_brand`, `test_put_cannot_move_inspiration_to_another_brand`, `test_patch_cannot_change_source_provenance`, `test_signal_inspiration_is_immutable`, `test_patch_to_specific_elements_without_focus_areas_is_rejected` (PR1-008/PR1-009) | PASS |
| RBAC-01 | Viewer mutates | Yes | Deny | `test_viewer_cannot_create_inspiration`, `test_viewer_cannot_upload_inspiration`, `test_viewer_is_denied_on_every_mutation_path`, `test_viewer_cannot_archive_inspiration`, `test_viewer_cannot_create_signal`; positive control `test_viewer_can_read` | PASS |
| LIFE-01 | Direct PATCH of a protected status | Yes | Reject/ignore, state unchanged | `test_patch_cannot_set_lifecycle_or_analysis_status` (row stays ACTIVE/NOT_ANALYSED), `test_patch_cannot_convert_ai_signal_to_user_origin` (row stays AI/PENDING) | PASS |
| LIFE-02 | Invalid transition | Yes | Reject | `test_archive_twice_is_rejected` (400) | PASS |
| LIFE-03 | Archived/revoked object reprocessed or extended | Yes | Reject | `test_analyze_archived_inspiration_is_rejected`, `test_archived_inspiration_cannot_receive_new_signals`, `test_archived_source_cannot_start_a_new_inspiration`, `test_ai_signal_rejected_for_archived_inspiration` (PR1-010) | PASS |
| LIFE-04 | Hard delete destroys provenance | Yes | Deny | `test_delete_inspiration_is_disabled`, `test_delete_signal_is_disabled` (405, rows still present) | PASS |
| IDEM-01 | Same analysis recorded twice | Yes | No duplicate permanent effect | `test_record_ai_signal_is_idempotent` (one AI row, payload refreshed), `test_duplicate_ai_signal_rows_are_rejected_by_the_database` (DB constraint `uniq_ai_signal_per_inspiration_attribute`), `test_reanalysis_does_not_reset_a_user_verdict` | PASS |
| INT-01 | Self-reference / supersession cycle | Yes — as of the CTO rework | Reject | Superseded by INT-02 below: PR2 now HAS a supersession chain (`superseded_by`), so PR1-011 applies to it and is enforced by a CHECK constraint plus a service-level guard. | PASS (see INT-02) |
| PROV-01 | Client mints a USER-origin signal for an inference, or converts AI → USER | Yes | Impossible | `test_payload_cannot_mint_an_ai_signal` (payload `origin=AI`/`extracted_by_provider` ignored), `test_patch_cannot_convert_ai_signal_to_user_origin`, `test_confirming_an_ai_signal_keeps_it_ai_derived` | PASS |
| PROV-02 | AI inference silently overwrites an explicit user preference | Yes | Impossible; disagreement surfaced | `test_ai_signal_does_not_overwrite_user_signal` (user row byte-for-byte unchanged; AI row flagged `conflicts_with` and ineligible), `test_same_sentiment_different_value_conflicts`, `test_different_sentiment_same_value_conflicts`, `test_agreeing_ai_signal_is_not_flagged_as_conflict` (no false conflicts) | PASS — **previously a false PASS.** Conflict compared `sentiment` alone, so an inference proposing a different value under the same sentiment slipped through, and the cited test asserted that as correct. Both are fixed; see `docs/reviews/PR02_CTO_REWORK_INTAKE.md`. |
| PROV-03 | Sentiment inferred from weight rather than stated | Yes | Reject / keep explicit | `test_sentiment_is_required` (400), `test_weight_does_not_imply_sentiment` (weight 0.95 + DISLIKED stored as DISLIKED), `test_weight_out_of_range_is_rejected` | PASS |
| STATE-01 | Stub or failure reports success | Yes | Impossible | `test_analyze_is_not_implemented` (501, `success=false`, `analysis_status` still NOT_ANALYSED). No code path can set `analysis_status=READY` in PR2 — `grep -n "AnalysisStatus" apps/inspirations/*.py` shows reads only, never an assignment other than the model default. | PASS |
| STORE-01 | Storage failure leaves a dangling row | Yes | Honest failure, no row | `StorageError` → `502` before `BrandInspiration.objects.create` in `apps/inspirations/views.py` `upload()`; the create call is after the try/except and the ordering is what makes it true. Not separately unit-tested — storage is stubbed under `STORAGE_TEST_MODE` and cannot be made to fail without patching. | PASS (code path + ordering); no automated failure-injection test |
| STORE-02 | Client points a row at another tenant's storage object | Yes | Impossible | `test_client_cannot_set_storage_coordinates` (submitted `storage_path`/`file_url` are dropped); upload prefixes with the resolved workspace id (`test_upload_stores_reference_and_server_assigns_storage` asserts the workspace id is in the URL) | PASS |
| URL-01 | Untrusted reference URL causes SSRF | Yes | Blocked/controlled | Nothing in PR2 fetches `reference_url`; it is stored as a string and validated only as a URL by `URLField`. Verified by `grep -rn "requests\|urlopen\|httpx" apps/inspirations/` → no matches. Fetching is introduced in PR6 and must add egress controls there. | PASS for PR2 — deferred control documented for PR6 |
| AI-01 | Provider receives the wrong tenant's context | No | — | PR2 makes no provider calls. `apps/inspirations` imports nothing from `apps.ai` (`grep -rn "apps.ai" apps/inspirations/` → no matches). The provider boundary is PR5/PR9 work. | N/A — no provider call exists in this PR |
| AUTH-01 | Two contradictory stated preferences held simultaneously for one attribute | Yes | Impossible | `test_database_refuses_two_authoritative_user_signals` (partial unique constraint `uniq_authoritative_user_signal` raises `IntegrityError`), `test_latest_user_signal_wins_and_history_is_auditable`, `test_at_most_one_signal_is_retrievable_per_attribute` | PASS |
| AUTH-02 | Case or whitespace splits one preference into two authorities | Yes | Impossible | `test_attribute_normalization_keeps_one_authority`, `test_value_normalization_ignores_case_and_whitespace` — folded columns are written in `save()`, so no writer can bypass them | PASS |
| AUTH-03 | Confirming a contradicting inference leaves the contradicted preference active | Yes | Explicit supersession | `test_confirming_conflicting_ai_direction_supersedes_the_preference`, `test_confirm_uses_current_authority_not_a_stale_pointer` (the row retired is the one actually authoritative at confirm time, not a stale `conflicts_with` pointer) | PASS |
| AUTH-04 | A retried analysis resurrects a superseded preference | Yes | Impossible | `test_ai_retry_cannot_resurrect_an_older_user_preference` (job re-run twice; the superseded row stays superseded and out of retrieval) | PASS |
| AUTH-05 | A human verdict ends up attached to content the human never saw | Yes | Impossible | `test_reanalysis_supersedes_rather_than_rewriting_a_judged_inference` (a changed inference under an existing verdict is filed as a new row) | PASS |
| AUTH-06 | Rejecting a preference silently revives the one it replaced | Yes | No resurrection | `test_rejecting_the_authority_does_not_revive_its_predecessor`, `test_rejecting_the_authority_releases_a_held_inference` | PASS |
| HIST-01 | PATCH rewrites a stated preference, destroying the audit trail | Yes | Reject | `test_a_stated_preference_cannot_be_edited` (400 on category/attribute/value/sentiment), positive control `test_weight_and_confidence_remain_editable` | PASS |
| HIST-02 | Client forges the supersession trail or the folded keys | Yes | Ignored, server-computed | `test_client_cannot_supply_the_supersession_trail` | PASS |
| HIST-03 | Superseded history leaks across tenants | Yes | 404 / not listed | `test_superseded_signals_stay_inside_their_tenant` | PASS |
| INT-02 | Signal supersedes itself, or a supersession cycle forms | Yes | Reject | `test_database_refuses_self_supersession` (CHECK `inspiration_signal_no_self_supersession`); longer cycles are unreachable because superseding a row deactivates it and only active rows are ever superseded | PASS |
| DET-01 | The per-row verdict disagrees with the retrieval query | Yes | Impossible | `test_row_verdict_and_queryset_agree` — every row in a fixture containing superseded, rejected, conflicting, agreeing-AI and current rows is checked both ways | PASS |
| MODEL-01 | ORM writer (job, command, service) creates a row whose brand is in another workspace | Yes | Refuse the write | `test_model_refuses_a_brand_from_another_workspace` — `BrandInspiration.save()` raises `ValidationError`; test asserts no row exists | PASS |
| MODEL-02 | ORM writer attaches a source from another brand or workspace | Yes | Refuse the write | `test_model_refuses_a_source_from_another_brand`, `test_model_refuses_a_source_from_another_workspace` | PASS |
| RBAC-02 | Viewer reaches a mutation path with no individually named test | Yes | Deny, table did not change | `test_viewer_is_denied_on_every_mutation_path` — eight paths (PATCH/PUT/DELETE inspiration, analyze, PATCH/confirm/reject/DELETE signal) each asserted for 403 and row-count stability | PASS |
| IDEM-02 | A named action is replayed | Yes | No compounded effect | `test_repeating_archive_does_not_compound` (state identical after the second call, second call 400), `test_repeating_analyze_does_not_compound` (still NOT_ANALYSED, still zero signals, second call 501) | PASS |
| BILL-01 | Unmetered AI spend | No | — | No AI capability is invoked, so no usage record is due. Metering for inspiration analysis is PR6 work. | N/A — no billable operation in this PR |
