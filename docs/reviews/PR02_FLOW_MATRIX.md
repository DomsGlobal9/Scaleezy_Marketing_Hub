# PR02_FLOW_MATRIX.md

## Purpose
Prove that no functionality exists only in one happy-path implementation.

All tests below live in `Marketing_backend/apps/inspirations/tests.py`.

| User capability | Entry point(s) | Permission | Validation | Persistence | Async/service | Success state | Failure state | Downstream | Test |
|---|---|---|---|---|---|---|---|---|---|
| Add a URL/social reference to a brand | `POST /api/marketing/inspirations/` | `IsAuthenticated` + `IsWorkspaceMember` + `HasWorkspaceRole` (EDITOR) | `BrandInspirationSerializer.validate` → `validate_reference_graph` | `BrandInspiration` row, `workspace`/`created_by` server-set | none | `201` + `analysis_status=NOT_ANALYSED`, `retrieval_eligibility.eligible=true` | `400` validation, `403` role, `404` unknown workspace | `BrandInspiration.objects.eligible_for_retrieval()` (PR5 consumer) | `test_create_inspiration_with_source_provenance` |
| Upload an image/screenshot reference | `POST /api/marketing/inspirations/upload/` (multipart) | same | `BrandInspirationUploadSerializer` → same `validate_reference_graph` | same row + server-assigned `file_url`/`storage_path` | `SupabaseStorageService.upload_and_describe` | `201` with envelope `{success, data}` | `400` validation before any upload; `502` on `StorageError` with **no row written** | same | `test_upload_stores_reference_and_server_assigns_storage` |
| Say what part of a reference to use | `POST`/`PUT`/`PATCH` inspirations | EDITOR | `usage_scope` + `focus_areas` cross-check | `usage_scope`, `focus_areas` | none | `201`/`200` | `400` when scope and focus areas disagree | context assembly (PR5) | `test_partial_usage_scope_expresses_use_only_typography`, `test_specific_elements_requires_focus_areas`, `test_full_reference_rejects_focus_areas`, `test_unknown_focus_area_is_rejected`, `test_usage_scope_can_be_widened_back_to_the_whole_reference` |
| Annotate a reference in the user's own words | `PATCH /inspirations/{id}/` | EDITOR | brand/source immutability guard | `annotation` | none | `200` | `400` if the same request also moves brand/source | — | `test_patch_annotation_is_allowed`, `test_patch_cannot_move_inspiration_to_another_brand` |
| State a liked/disliked/neutral preference | `POST /api/marketing/inspiration-signals/` | EDITOR | `InspirationSignalSerializer.validate` (tenant + archived parent + append-only guard) | `services.record_user_signal()` in one transaction: retires the current authority, inserts the new row with `origin=USER`/`CONFIRMED`, links the supersession, reconciles the live inference | none | `201`, and the replaced preference is now history | `400` missing sentiment / archived parent / foreign inspiration / edit of a stated preference | signal retrieval (PR5) | `test_create_signal_is_user_origin_and_confirmed`, `test_sentiment_is_required`, `test_latest_user_signal_wins_and_history_is_auditable` |
| Record an inferred preference | internal `services.record_ai_signal()` | internal only — no API path mints `origin=AI`; `BrandInspiration.save()` enforces the tenancy invariant for ORM writers | archived-parent guard; conflict re-derived against the current authority on folded value **or** sentiment | `InspirationSignal`, `origin=AI`, `user_confirmation=PENDING`; unchanged → refresh incidentals, changed+unjudged → rewrite in place, changed+judged → new row superseding the old | idempotent on `(inspiration, category, normalized_attribute)` | returns the live inference | `InspirationSignalError` for archived parent; `IntegrityError` on a duplicate raw insert | not retrievable while it conflicts, or while a stated preference exists for the attribute | `test_ai_signal_does_not_overwrite_user_signal`, `test_same_sentiment_different_value_conflicts`, `test_different_sentiment_same_value_conflicts`, `test_record_ai_signal_is_idempotent`, `test_reanalysis_supersedes_rather_than_rewriting_a_judged_inference`, `test_ai_retry_cannot_resurrect_an_older_user_preference`, `test_ai_signal_rejected_for_archived_inspiration`, `test_duplicate_ai_signal_rows_are_rejected_by_the_database` |
| Agree/disagree with a signal | `POST /inspiration-signals/{id}/confirm/`, `/reject/` | EDITOR | object must be in the caller's workspace (queryset scope) | `services.confirm_signal()` / `reject_signal()`: `user_confirmation`, `confirmed_by`, `confirmed_at`, `origin` untouched; confirming a contradicting inference also supersedes the current authority | none | `200` + serialized signal; the response says when a preference was superseded | `403` viewer, `404` other tenant | rejected and superseded signals leave `eligible_for_retrieval()`; the live inference is reconciled | `test_confirming_an_ai_signal_keeps_it_ai_derived`, `test_confirming_conflicting_ai_direction_supersedes_the_preference`, `test_confirm_uses_current_authority_not_a_stale_pointer`, `test_rejected_signal_is_ineligible`, `test_rejecting_the_authority_does_not_revive_its_predecessor`, `test_viewer_is_denied_on_every_mutation_path` |
| Revoke a reference | `POST /inspirations/{id}/archive/` | EDITOR | already-archived guard | `lifecycle_status`, `archived_by`, `archived_at` | none | `200` + honest `retrieval_eligibility` | `400` double archive, `403` viewer, `404` other tenant | excluded from `eligible_for_retrieval()`; its signals follow | `test_archive_makes_inspiration_ineligible`, `test_archive_twice_is_rejected`, `test_signals_of_archived_inspiration_are_ineligible` |
| Ask for analysis | `POST /inspirations/{id}/analyze/` | EDITOR | archived guard | none — nothing is written | deferred to PR6 | there is no success state | `501 NOT_IMPLEMENTED`, `400` if archived | — | `test_analyze_is_not_implemented`, `test_analyze_archived_inspiration_is_rejected` |
| Read what a retrieval step would see | `GET /inspirations/?eligible_only=true`, `GET /inspiration-signals/?eligible_only=true` | VIEWER | — | — | — | `200` list matching `eligible_for_retrieval()` — at most one signal per `(inspiration, category, normalized_attribute)` | — | PR5 gateway uses the same queryset | `test_eligible_only_filter_matches_the_retrieval_rule`, `test_at_most_one_signal_is_retrievable_per_attribute`, `test_row_verdict_and_queryset_agree` |
| Delete a reference or signal | `DELETE` on either detail route | EDITOR | — | nothing is deleted | — | none | `405` with a pointer to archive/reject | provenance preserved | `test_delete_inspiration_is_disabled`, `test_delete_signal_is_disabled` |

## Cross-path parity
For each rule, list every path that must enforce it.

| Rule | JSON | Multipart | PUT | PATCH | Action | Job/Internal | Proven equivalent? |
|---|---:|---:|---:|---:|---:|---:|---:|
| Tenant isolation | workspace-scoped `brand`/`source` querysets + `validate_reference_graph` + workspace-scoped viewset queryset | same function, plus workspace-scoped relation querysets | queryset scope + validate | queryset scope + validate | `get_object()` on the scoped queryset | `BrandInspiration.save()` refuses a mismatched brand/source; `record_ai_signal` takes an already-scoped object | YES — `test_model_refuses_a_brand_from_another_workspace`, `test_model_refuses_a_source_from_another_brand`, `test_model_refuses_a_source_from_another_workspace`, `test_cross_tenant_brand_injection_blocked`, `test_cross_tenant_source_injection_blocked`, `test_upload_cross_tenant_brand_injection_blocked`, `test_upload_cross_tenant_source_injection_blocked`, `test_every_mutation_path_is_404_for_another_tenant`, `test_signal_cannot_attach_to_other_tenant_inspiration` |
| Brand integrity (same-workspace, cross-brand) | `validate_reference_graph` source/brand check | same function | brand change rejected | brand change rejected | no action changes brand | signals carry no brand column | YES — `test_cross_brand_source_injection_blocked_inside_one_workspace`, `test_upload_cross_brand_source_injection_blocked`, `test_patch_cannot_move_inspiration_to_another_brand`, `test_put_cannot_move_inspiration_to_another_brand` |
| RBAC | EDITOR to write | EDITOR to write | EDITOR | EDITOR | EDITOR for archive/confirm/reject | n/a | YES — `test_viewer_is_denied_on_every_mutation_path` (eight paths, table-driven), `test_viewer_cannot_create_inspiration`, `test_viewer_cannot_upload_inspiration`, `test_viewer_cannot_archive_inspiration`, `test_viewer_cannot_create_signal`, `test_viewer_can_read` |
| Lifecycle | status fields read-only | not settable | read-only | read-only | archive/confirm/reject are the only transitions | `record_ai_signal` never changes a human verdict | YES — `test_patch_cannot_set_lifecycle_or_analysis_status`, `test_patch_cannot_convert_ai_signal_to_user_origin`, `test_reanalysis_does_not_reset_a_user_verdict` |
| Preference authority (one active preference per attribute) | `record_user_signal` supersedes the previous one | n/a — no signal path | append-only guard rejects the edit | append-only guard rejects the edit | confirm supersedes a contradicted preference; reject leaves the attribute without one | `record_ai_signal` reconciles but never writes a USER row | YES — `test_latest_user_signal_wins_and_history_is_auditable`, `test_database_refuses_two_authoritative_user_signals`, `test_a_stated_preference_cannot_be_edited`, `test_confirming_conflicting_ai_direction_supersedes_the_preference`, `test_at_most_one_signal_is_retrievable_per_attribute` |
| Revocation eligibility | archived source rejected at create | archived source rejected at create | n/a | n/a | archive sets it; `eligible_for_retrieval()` reads it | AI signals refused on archived parents | YES — `test_archived_source_cannot_start_a_new_inspiration`, `test_archived_source_makes_inspiration_ineligible`, `test_archive_makes_inspiration_ineligible`, `test_inspiration_without_a_source_stays_eligible`, `test_ai_signal_rejected_for_archived_inspiration` |
| Provenance origin (AI vs user) | `origin` read-only, server-set to USER | n/a (no signals on this path) | read-only | read-only | `confirm` records the verdict, never the origin | only `record_ai_signal` writes `origin=AI` | YES — `test_payload_cannot_mint_an_ai_signal`, `test_patch_cannot_convert_ai_signal_to_user_origin`, `test_confirming_an_ai_signal_keeps_it_ai_derived` |
| Storage boundary | `storage_path`/`file_url` read-only | server-assigned from a workspace-prefixed upload | read-only | read-only | n/a | n/a | YES — `test_client_cannot_set_storage_coordinates`, `test_upload_stores_reference_and_server_assigns_storage` |
